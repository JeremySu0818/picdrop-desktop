{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the PicDrop main window.",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-save",
    "fs:allow-write-file",
    "fs:allow-download-write",
    "fs:allow-picture-write"
  ]
}
